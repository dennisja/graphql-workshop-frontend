const ROUTES = {
  home: "/",
  register: "/register",
  links: "/links",
  createLink: "/add-link",
  search: "/search",
  users: "/users"
};

const AUTH_TOKEN = "AUTH_TOKEN";

export { AUTH_TOKEN, ROUTES };
